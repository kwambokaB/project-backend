import express form 'express' ;
import mongoose from 'mongoose';
import { verify } from 'jsonwebtoken';
import 

const route = express.Router();

route.get('/questions', verifyToken, (req,res)=>{
     
    const 

})
 
